---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: managedbgppeers.bgp.network.redhat.com
spec:
  group: bgp.network.redhat.com
  names:
    kind: ManagedBGPPeer
    listKind: ManagedBGPPeerList
    plural: managedbgppeers
    singular: managedbgppeer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.suspended
      name: Suspended
      type: boolean
    - jsonPath: .status.peerCount
      name: Peers
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedBGPPeer is the Schema for the managedbgppeers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedBGPPeerSpec defines the desired state of ManagedBGPPeer.
            properties:
              backend:
                description: Backend describes how to reach and authenticate with
                  the external BGP system.
                properties:
                  azureRouteServer:
                    description: AzureRouteServer configures Azure Route Server peering
                      via Virtual Hub BGP connections.
                    properties:
                      resourceGroup:
                        description: ResourceGroup is the resource group containing
                          the Route Server.
                        minLength: 1
                        type: string
                      routeServerName:
                        description: RouteServerName is the Azure Route Server (Virtual
                          Hub) resource name.
                        minLength: 1
                        type: string
                      subscriptionID:
                        description: SubscriptionID is the Azure subscription containing
                          the Route Server.
                        minLength: 1
                        type: string
                    required:
                    - resourceGroup
                    - routeServerName
                    - subscriptionID
                    type: object
                  type:
                    description: Type identifies the backend implementation.
                    enum:
                    - AzureRouteServer
                    type: string
                required:
                - type
                type: object
              debounceSeconds:
                default: 5
                description: DebounceSeconds is reserved for future event batching.
                minimum: 1
                type: integer
              machineNamespace:
                default: openshift-machine-api
                description: MachineNamespace is where OpenShift Machine objects are
                  managed.
                type: string
              nodeSelector:
                description: NodeSelector selects cluster nodes whose InternalIP addresses
                  become BGP peers.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              peerASN:
                description: PeerASN is the autonomous system number of the cluster-side
                  BGP speaker (e.g. FRR).
                format: int64
                maximum: 4294967295
                minimum: 1
                type: integer
              reconcileIntervalSeconds:
                default: 60
                description: ReconcileIntervalSeconds is the interval between reconciliation
                  passes.
                minimum: 5
                type: integer
              requireReadyNodes:
                default: true
                description: RequireReadyNodes skips nodes that are not in the Ready
                  condition.
                type: boolean
              suspended:
                description: Suspended triggers cleanup of managed external peers
                  and pauses reconciliation.
                type: boolean
            required:
            - backend
            - nodeSelector
            - peerASN
            type: object
          status:
            description: ManagedBGPPeerStatus defines the observed state of ManagedBGPPeer.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last successful
                  reconciliation.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              peerCount:
                description: PeerCount is the number of active managed peers.
                type: integer
              peers:
                description: Peers lists the current managed peer details.
                items:
                  description: ManagedPeerStatus describes one managed external BGP
                    peer.
                  properties:
                    nodeName:
                      description: NodeName is the Kubernetes node name used as the
                        Azure peering name.
                      type: string
                    peerBGPState:
                      description: PeerBGPState reflects the observed BGP session
                        state when available.
                      type: string
                    peerIP:
                      description: PeerIP is the node InternalIP configured on the
                        remote side.
                      type: string
                    peerName:
                      description: PeerName is the name of the external BGP connection
                        resource.
                      type: string
                    provisioningState:
                      description: ProvisioningState reflects the Azure provisioning
                        state when available.
                      type: string
                  required:
                  - nodeName
                  - peerIP
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
